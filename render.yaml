services:
  - type: web
    name: workforceiq-backend
    runtime: python
    plan: free
    rootDir: .
    buildCommand: "pip install -r requirements.txt"
    startCommand: "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: /health
    envVars:
      - key: ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: workforceiq-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: CORS_ORIGINS
        sync: false  # set to your deployed Vercel URL, e.g. https://your-app.vercel.app
      - key: SEED_ADMIN_EMAIL
        sync: false
      - key: SEED_ADMIN_PASSWORD
        sync: false
      - key: SEED_MANAGER_PASSWORD
        sync: false
      - key: HR_DATA_CSV_PATH
        value: ../datasets/hr_employee_data.csv
      - key: MCP_SERVER_PORT
        value: "8001"
      - key: MCP_SERVER_URL
        value: http://127.0.0.1:8001/mcp
      - key: PYTHON_VERSION
        value: "3.11.9"

databases:
  - name: workforceiq-db
    plan: free
    databaseName: workforceiq
    user: workforceiq
